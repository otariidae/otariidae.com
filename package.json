{
  "name": "otariidae.com",
  "private": true,
  "scripts": {
    "clean": "rm -rf _site",
    "prebuild": "npm run clean",
    "build": "eleventy",
    "predev": "npm run clean",
    "dev": "eleventy --serve",
    "format": "prettier --write \"**/*.{liquid,md}\"",
    "format:check": "prettier --check \"**/*.{liquid,md}\""
  },
  "author": "otariidae",
  "license": "CC-BY-4.0",
  "type": "module",
  "dependencies": {
    "@11ty/eleventy": "^3.1.5",
    "@11ty/eleventy-plugin-syntaxhighlight": "^5.0.2"
  },
  "devDependencies": {
    "@shopify/prettier-plugin-liquid": "^1.10.2",
    "prettier": "^3.8.3"
  }
}
